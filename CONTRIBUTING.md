# 参与贡献

感谢帮助测试星汐。公开测试阶段最有价值的是可复现问题、脱敏日志和明确的插件组合信息。

## 提交 Issue

- Bug 使用仓库的 Bug Report 模板。
- 一个 Issue 尽量只描述一个问题。
- 搜索已有 Issue，避免重复。
- 不要公开 API Key、Cookie、真实 QQ 号、群号、私聊或他人记忆。
- 无法公开的安全问题请按照 `SECURITY.md` 私下报告。

## 提交代码

1. 从新分支开始，保持改动范围单一。
2. 新行为应补充或更新测试。
3. 不把 `__pycache__`、日志、真实配置、表达向量和本地数据提交到仓库。
4. 保持 Python 3.12 兼容。
5. 提交前运行：

```bash
PYTHONPATH=.. python -m unittest discover -s tests -v
python -m compileall -q main.py core tests
```

涉及权限、工具过滤、身份识别或跨群记忆的修改，还必须在真实测试群验证；自动化 Stub 不能替代平台实测。

## 设计原则

- 口语化只改变“怎么说”，不改变“谁在说”和“事实属于谁”。
- 身份与权限由代码裁决，不依赖模型自觉。
- 普通用户权限默认拒绝；只读工具必须显式加入白名单。
- 长度使用软目标，不做破坏句子的固定字符截断。
- 单个 Provider 或辅助组件失败时优先安全降级。
