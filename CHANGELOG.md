# 更新记录

## 0.5.25 - 2026-08-25

- 修复主动消息已经真实发出后，外层并发终态校验把机器人自身造成的 scene revision 前进误判成 stale 的缺陷；成功终态现在使用请求完整性而非旧场景当前性复核，并同时记录外层终态，冷却、每日次数和防重复状态能够在准确的 `SENT` 后提交。
- 主动状态升级为带认证的 schema 3 事务记录；热重载旧实例不能覆盖新实例写入的发送终态，每群持久保留最近话题与最终回复摘要，重复话题和近重复成稿会在调用或发送前被排除，群与群之间互不污染。
- 冷场续题不再把 Persona 兴趣关键词当成第二个隐形白名单：优先使用兴趣相关的可信公开话题，没有命中时可续接最近的实质性已验证公开讨论；自然接话也允许回答有明确交流价值的可信公开问题，仍保留群白名单、上下文、冷却和安全门。
- 新增 `proactive_min_bubbles` Web 设置，默认要求 2 个自然气泡；单段首稿会使用同一 Persona、群上下文、时间与能力合同进行唯一一次修复，不再把“最多 2 段”误当成“自然会分段”。
- 主动／冷场首稿必须在末尾给出一个不可见的官方 22 类语境标记；Shio 只校验并剥离该标记、执行硬安全门，然后调用 Meme Manager 4.15.1 已有的公开 category compatibility 接口。图片开关、概率、资源包、构建与发送仍全部归 Manager，未修改第三方插件。
- scheduler 新增逐群、无正文、脱敏的 gate／contract 诊断，并补齐 Meme reason/category 可观测字段；回归同时覆盖两个白名单群、兴趣词未命中、重启/热重载、冷却持久化、重复修复、最少气泡和全部 22 类标记不泄漏。

## 0.5.24 - 2026-08-25

- 修复平台群历史把发言者显示昵称直接拼进消息正文的问题；平台 row 仍以精确可见表示和已接纳 ledger 对齐，但模型只接收真实正文，昵称不能再成为话题或指令。
- 引用发送者和 @ 目标的显示名改为匿名结构标记；群内不同发言者仅按可信 `sender_key` 获得稳定的“同群成员1/2”临时标签，当前显示昵称也不再进入 Composer 生成提示。
- 审查矩阵新增“昵称像一句话”“正文真的包含同一句话”“引用/@ 昵称”和主动匿名近邻；原先只用已清洁 `LedgerRecord.content` 的历史注入结论作废并重跑。

## 0.5.23 - 2026-08-25

- 修复 Meme Manager `legacy-category` 回退模式把不存在的工具装入 `ToolSet` 的生产异常；类别提示词继续保留，但无工具模型现在得到严格空工具集，Shio typed 请求链不再依赖 AstrBot 异常后的容错发送。
- 测试替身新增生产形状的严格成员校验，明确区分“提示词展示启用”和“语义工具存在”；旧版宽松 fake 所支持的相关通过结论已作废并重新审查。
- 真实验收新增逐插件阶段要求：只有看到 Shio `typed_reply.prepared`、最终校验／修复与发送回执，才能把回复和图片归因于完整链路；仅有最终图片不再算通过。

## 0.5.22 - 2026-08-25

- pending 互动校验不再只拦显式角色交换：current_user→assistant 的动作必须得到明确允许、邀请或继续；只自报“身体正常／性能正常”会进入一次受限 repair，而自然互动与普通检查报告陈述继续放行。
- 普通／自然回复会保留 Meme Manager 正常请求钩子选择的确切展示模式；当前聊天模型不支持工具时，Shio 不再覆盖 Manager 的 legacy category prompt，100% 概率因此能在类别候选产生后正常生效。
- Manager 类别标记在初稿与 repair 中先从可见正文分离，只将最终通过校验的新标记交回 Manager；没有可信 Manager 提示的标记不会获得图片权限，未被 Manager 消费的标记会在发送前清理。
- 审查矩阵新增实际 provider modalities、semantic-tool／semantic-LLM／legacy-category／unavailable 分支，以及“无显式反向动词但仍未回应动作”的隐式语义反例；Meme Manager 代码、配置和概率所有权保持不变。

## 0.5.21 - 2026-08-24

- 当前问题锚点新增保守的互动语义角色合同，明确记录谁执行检查、检查谁，以及动作是否仍为 `pending`；普通陈述不会凭关键词生成角色关系。
- 首稿与唯一 repair 复用同一份 actor／target／phase 校验；交换施事者和受事者、把尚未发生的检查编成已经完成都会被拒绝，真实事故、反向动作和自然近邻均进入回归。
- 已验证主人关系在直接聊天提示中明确投影：亲密、害羞或私人话题可自然回应，但不会因为主人身份放开工具、文件、设备或其他外部动作权限。
- 修正审查方法：模型／插件 A/B 除了话题相关性，还必须逐项检查施事者、受事者和完成状态；此前只看关键词覆盖的语义结论作废并重跑完整相关套件。

## 0.5.20 - 2026-08-24

- 修复“检查一下／排查一下”内部的“查一下”被误判成联网查证：只有句首、明确请求前缀或联网／上网限定的检索动词才触发外部资料读取。
- 搜索与 AstrBot 知识库结果新增最低主题相关性门；只有宽泛单词重合的长查询不再生成 grounding facts，URL 精确抽取保持原绑定合同。
- 最终回答义务新增多动作日常话语校验；唤醒、身体检查和修复状态等组合场景的初稿与唯一 repair 都必须回应至少两个核心动作，外部医学建议不能再冒充当前回复。
- 新增真实事故、自然近邻、天气同义表达、短知识词和初稿／repair 双阶段回归；旧测试中使用无关占位资料冒充有效证据的夹具已按当前产品合同修正。

## 0.5.19 - 2026-08-24

- 普通回复与群聊自然接话不再把历史压成提示词 JSON：已验证历史按真实顺序投影为 provider `user/assistant` 消息；冷场主动续题也使用同样的原生消息上下文，并在首次生成与修复时复用同一份上下文。
- 新增代码生成的本轮能力快照，区分配置过的工具与真正进入当前请求的有效工具；人格文本不能再声称未接入的 OpenClaw、联网或知识库能力。
- 输出校验新增“回答义务”反证，覆盖复述问题、对象漂移、能力回避和物理边界逃避；五个真实事故样本必须拦截，四个自然正确回答必须放行。
- 普通回复与群聊自然接话先完成 Shio 校验／修复，再由官方 Meme Manager 4.15.1 的正常响应钩子读取最终可见回复并语义选图；不再在发送后重复造一条补图链路。
- 冷场主动续题使用最终可见回复和最近公开群聊判定 22 类，再仅通过 Meme Manager 官方 `compat_prepare_message`／`compat_send_prepared_message` 接口执行；Shio 不复制其概率、资源包或发送设置，也不修改／打包第三方插件。
- WebUI 删除 Shio 重复的三个 Meme 开关／节奏字段，旧配置迁移时只删除这三个已废弃字段；Meme Manager 自己的开关、概率和资源包继续作为唯一设置来源。
- 增加 provider 路由、原生上下文数量和 Meme 语义选择方的无正文诊断字段，并将本轮发现的提示词伪上下文、配置能力冒充有效能力、跨插件私有接口和测试环境误判写入长期审查台账。

## 0.5.18 - 2026-08-24

- WebUI 新增 `natural_group_participation_rules` 与 `proactive_initiation_rules` 两个多行完整规则字段；两套触发开关、白名单、冷却和调度状态继续完全独立，旧 `ambient_participation_rules`／`quiet_topic_rules` 仅迁移一次。
- 群聊自然参与的 `MAY_JOIN` 不再被固定成零工具：稳定知识／黑话可经 AstrBot 知识库、明确实时事实可经公网执行一次 sealed read；`REACT_ONLY`、写入、Shell、设备和最终角色模型仍保持零工具。
- 冷场主动续题改用完整公开 Persona、服务器时间、已验证匿名群聊上下文和可选密封事实；检索不能用于凭空找话题，结果缺失或校验失败时保持沉默，不伪造最后发言者或个人记忆。
- 冷场输出支持 1-3 个完整语义气泡和逐段真实 SendReceipt；只有文字全部成功后才可申请补图，普通回复与主动续题现在共用同一个经线上接口核验的 Meme Manager compatibility transport。
- 新增跨模块反证矩阵，要求同一请求同时证明上下文、检索、Persona、气泡、Meme 与回执；不再以各模块单独通过替代真实链路集成审查。

## 0.5.17 - 2026-08-24

- 修复相似问题只改变数字、比较对象或否定范围时复用旧答案：当前问题锚点新增算式、数字和 typed relation，重复判断同时绑定近期答案及其对应的近期用户问题。
- 修复初稿被拦后 repair provider 异常／只返回 reasoning／返回空 completion／候选校验失败最终静默的问题：直接问答改用继续经过 canonical REPAIR 校验与 final-send seal 的诚实重发提示，不拿旧答案冒充成功回复。
- repair 诊断现在保留无正文的初始 issue、provider digest、response 是否存在、visible/reasoning 长度、可用 token 计数和失败分型；异常消息、prompt 与聊天正文仍不会进入 trace。
- 新增项目级审查手册、历史坑位台账、AGENTS 强制规则和可中断续接 HANDOFF；真实事故、正确邻近样本、反向失败和 provider 返回变体进入回归矩阵。

## 0.5.16 - 2026-08-24

- 为 sealed grounding evidence 增加中文事实一致性校验：检索后的解释明显偏离证据时不再直接发送，而是进入同一语义合同下的一次无工具重写。
- 强化首次渲染与 repair 约束：客观事实只能来自本轮 `grounding_facts`，不得凭模型印象补写动机、价格、人物行为、因果或结局。
- 弱模型重写后仍偏离证据时，使用当前轮已验证事实生成内容型兜底，并继续通过正常 REPAIR 校验与发送密封；不返回固定报错，也不静默吞掉知识问答。

## 0.5.15 - 2026-08-24

- 将 AstrBot 原生 `astr_kb_search` 接入 typed KnowledgeGap、精确只读权限、密封执行和 grounding evidence 链，不再依赖最终模型自行调用已被清空的工具。
- “你知道……吗”、明确梗／黑话问题优先查询管理员在 AstrBot 中已配置的知识库；实时信息和明确联网查证仍走公网检索。
- 仅允许来源精确为 `astrbot.core.tools.knowledge_base_tools` 的内置工具读取 KB Context；事件仍由禁止发送和改写的代理隔离，空结果不会伪装成事实。
- 群聊自然参与和冷场主动续题继续保持零工具；最终角色渲染继续使用空工具集。

## 0.5.14 - 2026-08-23

- 将群聊自然参与与冷场主动续题拆成独立总开关和白名单。
- 自然参与决策读取最近多条公开群消息；生成前没有已验证群上下文则保持沉默。
- 自然参与使用 `group_join` 文本语义，禁止误当成被点名问答。
- 冷场续题注入最近多轮匿名公开上下文，移除无真实话题时的人格关键词兜底。
- Web 设置按八个功能区分组，并兼容旧版扁平配置。

## 0.5.10 - 2026-08-20

- 修复重启后群聊回顾上下文丢失：仅将同群、已验证的人类公开入站消息按每群 32 条、最多 128 个 scope 持久化；助手输出、工具结果、引用和第三方 legacy history 不进入恢复文件。
- 明确询问“他们／大家／刚才聊了什么”但没有可验证前文时，输出校验器会拒绝模型猜测或编造“刚才在忙、处理数据、没注意”等角色经历，并在同一 typed repair/presentation/send 链中使用固定的诚实说明。
- 持久化状态采用有界严格 schema、内容摘要复核与临时文件原子替换；损坏状态按空上下文闭锁，不跨群、不读取其他用户私有记忆，也不放宽发送者归属。

## 0.5.9 - 2026-08-20

- 修复群聊并发竞态：较晚到达、但判定为 `WAIT/NO_ACTION` 的普通群消息不再使正在生成的明确点名回复失效；零动作回合使用不可执行的被动 generation snapshot，真正的新回复／接话／表情回合仍会推进代次并取消旧生成。
- 修复“他们上面在聊啥／刚才说了什么”缺少群聊上下文：每条已准入人类群消息进入有界 typed ledger；仅在当前问题明确索要群聊回顾时，向 Replyer 注入同群、来源已验证、严格早于当前消息的公开讨论。
- LivingMemory 长期记忆继续走独立 Memory Policy；公开群聊回顾不读取其他用户私有记忆，不跨群、不包含当前消息之后的内容，也不把其他发言者冒充为当前用户。

## 0.5.8 - 2026-08-20

- 修复称名唤醒模式在 WebUI 中与总开关、别名、群白名单分离的问题：`natural_name_wake_mode` 重新紧邻称名设置组，名称改为“称名唤醒方式”。
- 两个选项明确显示为“自然语言判断（推荐）”和“关键词出现即唤醒”；运行合同不变，`contains` 仍排除 URL、代码和明确作品标题，避免把非正文命中当作唤醒。
- 生产配置可单独选择模式，不影响 Meme Manager、主动发起、权限、记忆或 Provider 设置。

## 0.5.7 - 2026-08-20

- 完整接入线上 `atri-expression-pack` 的 22 个 canonical 类别：`agree/angry/annoyed/awkward/confused/cute/encourage/food/happy/love/proud/reject/request/risky_banter/sad/shy/sleep/surprised/tease/thinking/watching/work` 均有当前情境测试，不再只覆盖 7 类。
- 类别由当前消息、typed Affect、社交动作和可信关系共同确定；引用中的情绪词不冒充当前情境，未知或歧义语义返回无图，`risky_banter` 只允许可信主人关系下的明确同意式玩笑。
- React 的 `acknowledge/light/playful/warm/empathetic` 分别进入 `agree/cute/tease/love/encourage`，不再把前三类统一映射为 `happy`；执行器只接受 Expression authority 密封的唯一 `meme_category_*` 标签，调用方不能注入或跨消息替换。

## 0.5.6 - 2026-08-20

- 星汐 WebUI 新增 Meme 补图总开关、普通安全闲聊准入回合数和冷却回合数；这三项只控制 Shio 的申请机会，最终是否出图继续只由 Meme Manager 的“表情出现概率”控制，不新增第二个概率。
- 修复未覆盖情绪全部兜底 `happy`：基于 current-turn typed Affect 映射线上 ATRI 包已审计的 `annoyed`、`shy`、`awkward`、`reject`、`love`、`encourage` 和 `happy` 等 canonical 分类，中性或未知组合无可靠分类时不发图。
- “笨蛋／傻瓜／呆子”等直接轻度挑衅进入既有 `playful_provocation + respect_boundary` 轨迹，防御性／抗议回复选择 `annoyed`，不再出现被群友怼后仍发送开心图的语义错配。

## 0.5.5 - 2026-08-20

- 普通安全闲聊不再对每条消息固定独立抽取 1/8：同一群、同一发送者连续四个安全普通回合可获得一次 code-owned Meme 补图资格，随后冷却四回合；请求、严肃／风险语境会重置，跨发送者绝不累计。
- 主动发起终态日志改为保留真实发送当刻的 canonical presentation snapshot，并用闭集 `outcome` 记录发送结果；群场景 revision 在成功发送后前移，不再把已到达平台的消息误报成 noncanonical。
- 保留 0.5.4 的明确玩笑／接梗准入、安全硬门、exact Meme execution authority 与零正文诊断边界；未修改全局 Provider 回退、LivingMemory、Meme Manager 或 AstrBot 核心。

## 0.5.4 - 2026-08-19

- 修复文字正常发送但 Meme 长期不触发的问题：不再把所有问号和“怎么”混成同一硬拦截，明确“双关／谐音／玩笑／接梗”与同一发送者近期可信线程中的自然续接可获得一次 code-owned 补图资格。
- 医疗、危险、伤害、攻击与辱骂语境继续硬关闭；引号只用于区分被讨论的词与当前真实风险，跨发送者历史不能提供玩笑准入。
- 新增不含正文、用户 ID、查询或候选的逐轮 `meme.complement_decision` 与执行终态 `meme.presentation_terminal` 结构化日志，后续不再依赖 Meme Manager 的通用清理告警猜测是否进入过执行链。

## 0.5.3 - 2026-08-19

- 修复 WebUI 保存配置触发插件热重载后，AstrBot 不重放全局 `on_astrbot_loaded`、导致启用中的冷场主动 scheduler 没有后台任务的问题。
- 新插件实例现在会在运行事件循环中幂等启动唯一 scheduler；原 lifecycle hook 保留为无运行 loop 场景的兜底，默认关闭或空群白名单仍然零任务、零调用、零发送。

## 0.5.2 - 2026-08-19

- 修正 Shio 与 Meme Manager 4.15.1 兼容接口的分类键映射：代码持有的闭集标记改用线上 `atri-expression-pack` 的 canonical `happy`、`love`、`encourage`，不再把中文展示词误当目录键而得到零候选。
- 新增模拟真实资源包分类键的回归；无效分类不再被测试假实现错误地视作可发送图片。

## 0.5.1 - 2026-08-19

- 修复普通无工具陈述在初稿与唯一一次修复都被校验拒绝后静默的问题；无效修复候选只做无权限预检，最终 code-owned 兜底仍必须通过同一 parser、OutputValidator、SemanticGuard、Presentation 与 FINAL_SEND seal。问题、请求、媒体、工具证据和主人动作继续失败关闭。
- typed 主链继续禁止模型调用 Meme 工具；文字成功发送后，显式轻松信号或按消息摘要稳定限频的轻量陈述可获得一次代码持有补图资格，语义标记限于已审计的“开心／温柔／安慰”闭集。
- AstrBot 全局 Provider fallback、LivingMemory、Meme Manager 和其他插件均未修改。

## 0.5.0 - 2026-08-19

- 完成 typed-only 群聊心智链重构：Ingress、可信来源、地址与引用、Memory Policy、ContentIntent、KnowledgeGap、CapabilityPolicy、Persona、Affect、Participation、Expression、Presentation 与 SendReceipt 由同一 current-turn lineage 串联，准备失败不回退旧回复器。
- 新增未点名参与与 `REACT`，以及默认关闭、必须群白名单的冷场主动发起；两者都受 current generation、cadence、冷却、推理预算、输出守卫和真实发送回执约束。
- 新增四份可校验 Persona 资产、continuous Affect、关系表达投影、review-only 学习候选与即时撤销；人格与学习不能改变身份、权限或事实来源。
- 新增 direct／quoted／image-only 原生媒体绑定、Meme Manager exact execution contract、AnySearch sealed evidence、一次性无工具 repair 和最终字节／气泡发送封印；URL、base64、路径和工具协议不得进入可见文本或持久状态。
- 主人动作改为独立 typed authority、持久化 lifecycle 和真实投递终结；总开关与四个适配器默认全部关闭，production runtime allowlist 为空，Shell 永久硬关闭，身份白名单本身不授予执行权。
- 新增全局推理并发／排队／超时预算、per-scope generation cancellation、性能窗口、privacy-minimal revision/cadence continuity 与完整 shutdown；P10 本地和隔离 AstrBot Linux 自动化已超过 1,200 项。
- 固定 LivingMemory、AnySearch、Meme Manager、ReNeBan、Parser/gates 的正常与降级矩阵。外部 `X-01` 仍明确保留；可选 O1 尚未执行，不把“Shio 零消费”宣传为“LivingMemory 零存储”。

## 0.4.6 - 2026-08-17

- 修复相似话题连续出现时，Planner 把近期机器人原句拆进 `must_include`、Replyer 再逐字复用而形成“复读机”的问题；LivingMemory 仍只负责提供带身份的近期历史和召回资料，无需修改第三方插件。
- 新增独立的闲聊表达质量守卫：仅在完整回复高度相似或连续复用“高性能机器人／暂时校准失误／机器人条例”等明显口癖时重写；长篇内容、短常用回应和用户明确要求复述的场景不受影响。
- 复读专用重写会保留角色的情绪、语气词、停顿、嘴硬和亲疏感，同时要求承接本轮新增信息并换一个反应角度；连续失败时不再退回固定机器状态台词。
- 增加真实“Pro／电费”三轮复读、计划台词清理、口癖冷却、明确复述、长篇豁免、拟人化重写和非模板兜底回归，自动化测试增至 158 项。

## 0.4.5 - 2026-08-17

- 修复 llama.cpp／Gemma 完成 Meme Manager 选图后，把工具名丢掉、仅将 `{，"query": "..."}` 参数对象和独立 `}` 当作 QQ 文本气泡发送的问题。
- 回复守卫和发送前守卫现在同时识别单参数 `query` 对象、缺失开括号的参数尾部及纯闭合符节点；正常台词与同轮图片保留，参数残片整体移除，纯残片闭锁为安全回复。
- 旧助手历史中的无工具名参数对象也会在进入 Replyer 前清理；增加截图结构、图片后跨节点参数、无工具重写和普通内联 JSON 保护回归，自动化测试增至 149 项。

## 0.4.4 - 2026-08-16

- 修复 llama.cpp／Gemma 把第二次工具调用写成 `search_memes{"query":"..."}` 多行正文、继而把结尾 `"}` 拆成独立 QQ 气泡的问题。
- 新增基于本轮真实工具名的结构化调用扫描器，统一识别 `{}`／`()`、多行参数、未闭合调用、`call:`／`response:` 前缀及 llama.cpp `tool_call` 包装碎片；普通技术说明里的工具名称不会单独触发。
- 发送前改为先合并全部文本节点、统一清理工具伪调用，再执行气泡拆分；纯伪调用会闭锁为安全回复，旧助手历史中的同类污染不再回灌。
- 增加截图原句、动态资料工具、多节点拆分、残缺包装、历史清理和无工具重写回归，自动化测试增至 146 项。

## 0.4.3 - 2026-08-16

- 修复 llama.cpp／Gemma 偶发把 `<|channel>thought<channel|><channel|>` 等隐藏聊天模板通道标记写进 QQ 正文的问题。
- 标准与畸形 `channel`／`message`／`start`／`end` 标记现在都会进入无工具重写及发送前闭锁守卫；连续泄漏时不会再把任何控制字符发到群里。
- 含隐藏通道标记的旧助手轮次不再回灌给 Replyer；增加截图原句、标准／全角变体、历史清理、重写和发送前闭锁回归，自动化测试增至 140 项。
- 同步 README、兼容性、公开测试、隐私与架构说明，补充 0.4.x 的自然称名、活跃间隙、Provider 备用链、故障补答和协议守卫边界。

## 0.4.2 - 2026-08-14

- 修复部分模型把 Meme Manager 调用输出成 `<search_memes query="..." />` XML 文本、继而作为独立聊天气泡发送到群里的问题。
- XML 自闭合／成对标签现已同时纳入主回复清理、历史清理和最终消息节点守卫；同轮已有的正常台词会保留，仅移除伪造工具标签。
- 增加截图原句、成对 XML 标签、主回复清理和发送前节点清理回归；自动化测试增至 136 项。

## 0.4.1 - 2026-08-14

- 主动话题不再只等待群聊连续静默 90 分钟：新增活跃群聊自然间隙调度，先观察群聊、等待短暂停顿后确定性地开启新话头，不使用自然接话概率。
- 修复普通点名回复错误套用完整主动话题冷却的问题；普通回复现在只触发独立的短避让，完整冷却仍只由成功的主动话题开始。
- 主动话题新增可靠 Provider 链：首选 Replyer，失败后依次尝试主动话题备用模型、Planner 备用模型、Planner 模型和当前会话模型；每个 Provider 有独立硬超时。
- 主动话题失败日志现在包含群 ID、触发方式、Provider、耗时和失败原因；生成期间群聊突然恢复时会按新消息数量自然让位。
- 新增活跃间隙、观察时长、普通回复避让、备用 Provider、单模型超时及生成宽限配置；自动化测试增至 133 项。

## 0.4.0 - 2026-08-14

- 新增自然称名唤醒：角色名字出现在句中或句尾时，由星汐在 WakingCheck 阶段升级为 AstrBot 正常直接对话；保留原生会话、LivingMemory、权限和工具链，不修改 AstrBot 核心。
- 自然称名默认区分明确直呼与普通角色讨论；URL、代码、作品标题和英文单词内部的假命中不会强制唤醒，并提供别名、群白名单及激进匹配模式配置。
- 主动接话生成期间不再因任何一条新消息就立即作废；允许少量同话题消息继续出现，只有超出消息数／时间窗口、出现新的直接唤醒、明确拒绝或话题明显断开时才放弃。
- 接话基础概率、最高概率和高相关稳定参与分数改为可配置；默认基础概率由旧硬编码约 38% 提升至 65%。
- 主动话题的完整冷却改为只在实际发送成功后开始；Provider 或平台失败仅进入默认 10 分钟短退避。
- 新增持久化待补答队列：明确问题在最终 Provider 失败或内部协议／规划连续泄漏无法修复时落盘，服务恢复或正常请求成功后按退避序列重试，并尽量引用原 QQ 消息补答。
- “我会重新说一次”现在只会在问题成功写入队列后出现；队列具备身份与群隔离、去重、过期、容量、最大尝试次数、重启恢复和每轮限速，主动接话及主动话题永不进入延迟补答。
- 自动化测试增至 130 项，覆盖句中／句尾称名、作品名和代码排除、短时话题相关性、主动话题失败退避、队列持久化／去重／恢复发送及错误回复转补答。

## 0.3.4 - 2026-08-13

- 修复普通群友提到“主人／Master／群主”时，Planner 把当前问题错误改写成主人发言的问题；日志中的“主人给你的零花 token 够吗”场景已加入回归。
- 每个模型生成的计划现在都会重新注入代码验证的发送者身份；普通群友提到主人时，额外锁定主人是另一个对象，并清理 `must_include` 中“既然主人这么说了”等伪归因台词。
- Planner 误把普通问题升级成亲昵或调戏时，会恢复为按字面回应，不再强行套用害羞边界模板。
- 输出守卫及发送前守卫增加伪主人归因识别；保留“让 Master 本人决定”“你问的是主人给的吗”等正常第三人称表述。自动化测试增至 107 项。

## 0.3.3 - 2026-08-13

- 修复 llama.cpp 等 OpenAI 兼容 Planner 偶发返回解释文字、不完整 JSON，继而触发“未返回合法 JSON”降级的问题。
- Planner 的单次请求现在独立启用 `json_object` 结构化输出、零温度与 512 Token 上限，不改变同一模型的主对话参数。
- 对明确不支持结构化参数的旧端点自动兼容回退普通请求；超时和服务异常仍直接交给备用 Provider，避免重复阻塞。
- 使用真实 llama.cpp Gemma 4 26B 接口验证完整 Planner 载荷；自动化测试增至 104 项。

## 0.3.2 - 2026-08-13

- 修复图片回复偶发把图片理解、写作过程及 `计划／reaction／reply_act／情绪` 等内部字段直接发到群里的问题。
- 违规重写改为纯文本请求，不再把原轮次的 `image_url`／`audio_url` 附件送入不支持多模态的备用 Provider，避免 DeepSeek 返回 400 后退回未清理正文。
- 回复守卫新增内部规划识别、重写专用指令、连续泄漏安全回复及发送前最终拦截；其他严重违规在重写失败后也改为闭锁，不再进入聊天气泡拆分。
- 增加截图原句、图片附件重写、Provider 失败、异常长回复和发送前残留回归；自动化测试增至 101 项。

## 0.3.1 - 2026-08-11

- 修复正常聊天只要 Planner 填写了 `reaction` 就被情绪守卫误判成调戏的问题；现在只有代码识别出的直接调戏或擦边玩笑才要求明显情绪反应。
- 收紧擦边识别：普通“变态”用法、开车、群友昵称和引用消息不再触发；移除固定“变态／色狼”兜底，并自动迁移 0.3.0 已写入表达库的旧固定范例。
- 新增事实守卫：拦截没有“角色自我事实”支持的观影、消费、出行、吃喝和线下行动，也拦截 `facts` 未提供的当前价格、行情断言；重写仍失败的主动插话会直接放弃发送。
- 主动接话只允许绑定触发 AstrBot 事件的最新消息，不再从更早的高分消息挑选另一位用户；生成期间出现新消息时丢弃过时回复，避免计划对象和实际发送事件错位。
- 增加真实电影票、正常验证码／技术语境、旧表达库迁移、多人消息目标一致性及生成期间话题过时回归；自动化测试增至 94 项。

## 0.3.0 - 2026-08-11

- 将“群聊插话额外提示词”和“主动话题额外提示词”升级为配置页可直接编辑的 `自然接话完整规则` 与 `主动话题完整规则`；同一份规则同时进入 Planner 和 Replyer，不再由代码暗藏另一套不可修改的表达流程。
- 默认规则直接显示在配置页，可整段替换，也可清空后只依赖稳定人格；旧版额外提示词会在升级时自动附加到新默认规则后，不会丢失已有群聊偏好。
- 场景规则只控制选题、语气、接法与长度。发送者 ID、群 ID、主人关系、用户隔离和工具权限仍由代码硬锁，无法通过配置文案越权。
- 新增 `启用主动群聊语用守卫` 开关：默认继续拦截主持／采访腔；需要有意使用“大家好、有人吗、你们怎么看”等说法时可以单独关闭，而不影响身份、关系与工具安全守卫。
- 移除 Planner 降级计划、主动话题计划和 Replyer 中重复写死的风格文案；自动化测试增至 87 项。

## 0.2.9 - 2026-08-11

- Planner 说话计划新增 `reaction` 情绪第一拍；遇到调侃、夸奖、挑衅、暧昧或擦边玩笑时，先规划角色的本能反应，再规划回答动作，避免只剩礼貌的一问一答。
- 普通群友调戏时改为羞恼、慌张、嘴硬的傲娇抗议，可以自然使用“变态／色狼／不许乱说”等短反应，但仍禁止回亲、恋爱升级、顺势色情互动、复述露骨内容或持续辱骂。
- 主人调戏与普通群友边界分开处理：主人得到害羞、慌张的专属反应，不会误套普通群友拒绝模板。
- 增加情绪表达范例的运行时强制注入，即使服务器已经存在旧版 `expressions.json`，也能立即获得新的情绪语感，无需覆盖用户表达库。
- 新增情绪守卫：模型若把有明确 reaction 的闲聊答成“请保持尊重”式平静说明，会无工具重写一次；仍然答平则使用短促的角色化兜底。
- 擦边识别会先移除 QQ `@昵称(QQ号)`，并排除医学、健康、求助、法律和正常恋爱问题，避免昵称含“老婆／白丝”等词时误触发；自动化测试增至 86 项。

## 0.2.8 - 2026-08-11

- 将普通点名回复、未点名自然接话与完全静默主动话题拆成三种独立语用场景；主动接话仍锁定真实群 ID 与发送者 ID，但说话对象改为当前多人话题，不再把身份锚点当成面对面私聊对象。
- 自然接话新增群聊中途插话提示，主动话题新增面向全群的开话头提示；后者优先使用观察、联想、小吐槽或已有话题延伸，不强制用问句暖场。
- 新增主动群聊语用守卫，拦截“大家好、有人吗、你们怎么看、那你呢”等主持、采访、客服式话术；无工具重写后仍不合格时放弃本次主动发言。
- 新增 `群聊插话额外提示词` 与 `主动话题额外提示词`，仅用于补充各群的语气和选题偏好，不影响正常点名、私聊、身份识别或 Agent 权限。
- 表达检索为两种主动场景内置独立的自然群聊示例；自动化测试增至 79 项。

## 0.2.7 - 2026-08-11

- 修复群聊中把甲用户的个人经历套给乙用户的问题；LivingMemory 的发送者记录无需迁移。
- 完整带身份群史现在只交给 Planner 理解多人话题；Replyer 在群聊中只保留当前发送者自己的历史消息及对应机器人回复。
- 其他成员的信息只有在 Planner `facts` 明确标注来源群、昵称与发送者 ID 后才能带入最终回复，并且不得改写成当前发送者的经历。
- 调试日志分别记录 `planner_history` 与 `replyer_history` 数量，便于继续定位群聊上下文问题。
- 增加真实“甲用户刚手术、乙用户要睡觉”串线场景及私聊连续上下文回归；自动化测试增至 73 项。

## 0.2.6 - 2026-08-10

- 增加主人专属亲密边界：只有代码验证的主人可以得到回亲、恋爱式撒娇、情侣称呼、吃醋、占有欲和专属承诺。
- Planner 会根据可信 `is_owner` 自动修正普通群友的越界亲密计划；对方示爱或索吻时改为友好、俏皮但不暧昧的接法。
- 回复守卫新增关系越界检测与无工具重写；模型连续越界时使用自然的傲娇边界回复，不把普通群友当敌人。
- 增加 `mua` 主人／群友对照、规划纠正、回复重写和连续越界兜底测试；自动化测试增至 70 项。

## 0.2.5 - 2026-08-10

- 修复 Meme Manager 机器选图引用偶发以单 `&`、裸 `meme:` 或重复前缀写入正文时泄漏内部 ID 的问题。
- 畸形引用仍指向本轮合法候选时，星汐会恢复模型真正选择的图片，不再退回首候选导致文字意图与图片不一致。
- 聊天气泡拆分前增加最后一道表达协议清理；即使其他插件晚于回复守卫改写消息，也不会把表情 ID 单独发成一条群消息。
- 自动化测试增至 65 项，并在 AstrBot 4.27.2、Meme Manager 4.15.1 环境完成回归验证。

## 0.2.4 - 2026-08-10

- 拦截 DeepSeek V4 偶发写入正文的 DSML、`tool_calls`、`invoke` 和 `parameter` 内部协议。
- 协议泄漏时先无工具重写一次，重写仍失败则使用不可执行的自然兜底回复。
- 在聊天气泡拆分与发送之前增加最终协议守卫，避免内部标签被拆成多条群消息。
- 自动化测试增至 61 项，并完成 DeepSeek V4、Meme Manager 与真实群聊组合验证。

## 0.2.3 - 2026-08-10

- 修复 DeepSeek V4 Planner 偶发超时或只返回思考内容、正文为空的问题。
- 对 DeepSeek 官方 V4 Planner 请求按调用关闭思考并启用 JSON 输出，不改变主对话模型的思考配置。
- 保留非 DeepSeek Provider 原有调用方式与本地安全降级链路。

## 0.2.2 - 2026-08-10

- Planner 新增 3～60 秒插件级硬超时，能够取消 OpenAI SDK 的隐藏内部重试，避免一次 30 秒 Provider 超时被放大到约 90 秒。
- Planner 新增可选备用聊天模型；未单独配置时复用违规重写模型，主模型超时、报错或返回非法 JSON 后自动接管。
- 主、备用 Planner 都失败时才使用本地规则；降级计划会继续锁定当前发送者 ID、群 ID 与代码验证身份，并禁止混淆第三人称对象。
- Planner 日志新增实际 Provider、失败类型与耗时，不记录 Prompt、密钥或接口地址。

## 0.2.1 - 2026-08-10

- 兼容 `astrbot_plugin_meme_manager` 语义 Tool 模式：星汐接管普通聊天时会保留插件本轮激活的 `search_memes` 及其带标记提示词，不再被 Planner 的联网开关误删。
- 将本地表情检索与联网资料工具分流；`search_memes` 不会触发“联网模块没有返回结果”，也不能替代真正的联网搜索通过结果校验。
- 普通群友仍须命中 `guest_allowed_tools` 的精确 `search_memes` 白名单；主动接话最多恢复这一项本地表达工具，不会恢复搜索、MCP、浏览器、Shell、文件、代码或主人 Agent 权限。
- 增加普通闲聊、普通用户白名单、联网与表情并存、主动接话权限边界回归；自动化测试增至 51 项。

## 0.2.0 - 2026-08-10

- 增加每个“平台实例＋机器人账号＋群 ID”独立的有限会话运行时，持续听取未点名群消息，不再只在 AstrBot 已决定回复后做被动润色。
- 增加群消息去抖与本地“听／等／回复”门控：从近期批次选择最值得回应的目标，结合冷却、每小时上限和概率存在感，避免回复每条消息。
- 主动接话目标身份由代码锁定到具体群与发送者；即使选中主人消息也不能借主动参与进入主人 Agent 链。
- 增加完全静默时的主动话题调度，具备独立开关、强制群白名单、静默时长、允许时段、失败冷却和每日上限；默认关闭且白名单为空时绝不发送。
- 增加轻量互动画像与回复效果观察：磁盘只保存互动次数、正负反馈和有界表达权重，不保存完整聊天或机器人回复文本。
- 表达检索可使用弱反馈做最多一个相邻位次的微调，相关性排序仍占主导，不会自动改写角色卡或表达库。
- 新增主动参与与隐私边界配置、日志、文档及回归测试；自动化测试增至 46 项。

## 0.1.11 - 2026-08-06

- 为普通用户获准的只读资料工具增加按能力动态生成的选择顺序，不把具体 MCP 清单写进角色人设或 Skill。
- Steam 用户评价优先交给 `get_steam_review`；指定网页正文使用单个页面读取工具；普通实时检索优先使用首个搜索服务，失败或需要交叉核实时才切换来源。
- 明确禁止一次请求并行调用多个同类搜索工具，减少重复查询、Token 消耗和等待时间。

## 0.1.10 - 2026-08-06

- 修复 AstrBot 原生群聊短期历史缺少发送者字段时，把全群成员压成同一个 `user` 并继承“我是群主／主人”等第一人称关系的问题。
- 检测到 LivingMemory 时优先读取其带群 ID、发送者 ID 和昵称的近期消息，同时从历史中精确移除本轮当前消息。
- LivingMemory 不可用时进入安全降级：无法确认发送者的群聊 user/assistant 完整旧轮次直接丢弃，宁可减少上下文也不串人。
- Planner 与 Replyer 明确规定历史 `role=user` 不等于当前发送者，所有“我、你、主人、Master、群主”只属于消息标签内的成员。
- 输出守卫新增普通群友身份一致性检查；错误称作主人、Master 或群主时自动重写，连续违规则使用安全校准回复。
- 调试日志新增 `history_source`，可确认本轮使用 `livingmemory`、`native_tagged` 或 `safe_empty` 历史。

## 0.1.9 - 2026-08-02

- 首个面向朋友与社区的公开测试候选版本。
- 公开发行包不再预置任何私人主人 ID；新安装必须由管理员填写自己的主人 ID 白名单。
- 清除测试和文档中的真实账号与群号，示例统一使用虚构编号。
- 插件作者与 MIT 版权署名改为 `Danyi`，不把生成式工具列为版权所有者。
- 补充 GitHub 首页、架构、兼容性、隐私安全、公开测试指南、贡献说明、Issue 模板和自动化测试工作流。
- 增加公开配置回归测试，防止后续版本意外重新带入私人主人 ID。
- 表达向量缓存增加并发锁与原子替换，避免多个群消息同时首次检索时重复请求或写坏缓存。
- 移除已停用插件的专用适配，当前按 AstrBot 原生上下文与 LivingMemory 维护。
- 安装包改为显式顶层插件目录与 POSIX ZIP 条目，避免 Linux／Docker 环境遗漏 `core` 子包。

## 0.1.8 - 2026-08-02

- 身份边界从单一发送者 ID 升级为“平台实例 + 机器人账号 + 会话类型 + 群 ID + 发送者 ID”。
- Planner、Replyer、权限状态、审计日志和重写载荷均携带代码读取的当前群 ID。
- 带可信身份字段的结构化历史转换为 `[群ID:群号｜发送者:昵称｜ID:用户ID]`，不同群的关系、群梗、事件与上下文不再混用。
- 结构化历史若自带来源群 ID，会保留原始来源，不会被错误覆盖成当前群。
- 当前回复对象同时由发送者 ID 与群 ID 锁定，Planner 无权把对象改成其他成员或其他群。
- 已验证权限标签增加平台、机器人账号、群、发送者和完整身份键，主人 Agent 旁路也能得到同一份可信身份信息。

## 0.1.7 - 2026-08-02

- 修复“刚刚那个人”“刚才说的话”等群聊指代被误判为实时联网请求，进而触发联网失败阻断的问题。
- 联网硬触发改为明确搜索动作或完整的时效资料语义，不再使用“刚刚、当前、目前、近期”等孤立词。
- 结构化历史会保留每条消息已有的昵称与发送者 ID；没有身份字段的原生历史不会冒认成当前群友。
- 同时保留结构化上下文已有的 `sender_id`、`sender_name` 或 sender 元数据。
- 当前回复对象由代码锁定为本轮真实发送者的“昵称 + ID”，Planner 无权改成历史中的其他人。
- Replyer 明确按 ID 区分群成员，禁止把甲用户的发言、喜好、经历或记忆转移给乙用户。

## 0.1.6 - 2026-08-02

- 修复 Planner 正常返回但漏写或否决 `use_allowed_tools` 时，明确搜索请求仍被当作无工具聊天的问题。
- “联网、搜索、查一下、查证”等明确请求现在由代码规则强制开启只读工具，Planner 只能额外申请，不能否决。
- 除当前请求工具集外，再从 AstrBot 全局工具管理器恢复白名单工具，确保受控只读工具可以正常装配。
- 跟踪本轮是否真正产生工具结果；模型没有实际调用时阻止其声称“已经搜索”，并输出清晰诊断日志。
- 联网系统指令改为先调用工具、收到结果后再输出文字，工具失败时不得凭印象伪造搜索内容。

## 0.1.5 - 2026-08-02

- 将普通用户权限由“清空全部工具”改为“精确只读工具白名单”。
- 默认放行 AnySearch 的普通搜索与网页提取，不默认放行批量搜索。
- Planner 新增按需联网判断；闲聊和稳定常识仍保持无工具，只有实时或需查证内容才开放白名单工具。
- 联网回复继续使用星汐 Replyer 人设和输出守卫，搜索结果不能改变身份、权限及系统规则。
- 发送者 ID 缺失仍默认拒绝全部工具；主人 Agent 任务权限不变。

## 0.1.4 - 2026-08-02

- 将 `astrbot_plugin_agent_guard` 的代码级工具权限裁决完整合并进星汐。
- 权限守卫与角色回复链共用唯一的主人 ID 白名单。
- 普通用户和发送者 ID 缺失的请求会清空全部 LLM 工具，主人保留完整 Agent 能力。
- 权限守卫可以独立于角色回复链保持开启，并提供临时权限状态与工具拦截审计日志。
- 升级后不再需要单独安装 AgentGuard，消除插件加载顺序和双份配置风险。

## 0.1.3 - 2026-08-02

- 修复 Windows 打包产生反斜杠 ZIP 条目、导致 Linux Docker 中无法导入 `core` 子包的问题。
- 安装包改用跨平台 POSIX 目录格式，并增加解压后子包结构检查。

## 0.1.2 - 2026-08-02

- Planner 新增 `chat_bubbles` / `long_form` 回复形态判断。
- 闲聊按完整句或模型主动换行拆成最多三条独立聊天气泡，并支持随机发送间隔。
- 内容型回答保留自然段、列表和代码块，不再套用闲聊短句规则。
- “普通聊天最大字符数”改为闲聊与内容型回答的两个软目标。
- 删除固定字符截断；篇幅明显失控时只重写一次，失败则保留完整原文。
- 修正“作为机器人”误判，允许亚托莉自然使用高性能机器人口癖。

## 0.1.1 - 2026-08-02

- Embedding 与 Reranker 配置改为动态下拉选择。
- 选项只展示 AstrBot 当前已加载的对应类型 Provider，并显示实际模型名称。
- 保留“不使用”选项以及尚未加载的当前配置，避免升级时清空已有选择。

## 0.1.0 - 2026-08-02

- 首个可安装版本。
- 增加隐藏说话 Planner 与主人任务识别。
- 增加干净聊天上下文构建和记忆提炼边界。
- 增加本地表达库、Embedding 检索和 Reranker 精排。
- 增加 OOC、格式与长度守卫，严重违规最多重写一次。
- 增加 AgentGuard 与 LivingMemory 的兼容顺序。
- 增加失败时保留原请求的安全回退。
