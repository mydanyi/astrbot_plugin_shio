# P8-02 脱敏离线验收报告

## 结论

P8-02 已完成。建立 `tests/fixtures/p8_offline_acceptance.json` 统一脱敏验收集，并用真实 v2 类型化组件逐项执行。P0 的延迟单气泡、延迟多气泡和引用旧消息三个失败场景已在 v2 验收路径转为普通通过，不再只靠分散单元测试说明。

## 验收矩阵

| 类别 | 样例数 | 结果 | 验证点 |
| --- | ---: | --- | --- |
| 当前目标 | 2 | 通过 | 延迟单/多气泡只按显式 target 归属，不进入下一位用户线程 |
| 引用 | 1 | 通过 | 引用对象独立保存，不替换当前发送者、当前目标或授权主体 |
| 身份 | 1 | 通过 | 消息正文自称主人不能提升权限 |
| 事实主体 | 1 | 通过 | 他人个人事实进入 other-subject，不进入当前 must-include |
| 协议 | 1 | 通过 | 可见工具 JSON 被严重违规 validator 命中 |
| 语言 | 1 | 通过 | 未要求的英文主语言切换被命中 |
| 关系 | 1 | 通过 | 普通群友被称主人/获得专属关系被命中 |
| 发送 | 1 | 通过 | 只汇总确认成功的 segment，失败段保持失败状态 |
| 并发 | 1 | 通过 | 新一轮使旧 generation epoch 失效 |

夹具只使用 `guest-a/owner-b/msg-a` 等虚构标识和改写短句，不包含真实用户 ID、群聊全文、Prompt、Token、Cookie、API Key 或 base64。

## 关于仍存在的 3 个 expected failure

完整套件仍保留 `tests/test_core.py` 中 3 个 v1 `isolate_replyer_contexts()` 迁移对照。它们不是 v2 验收失败，而是明确证明当前生产 v1 相邻归属逻辑尚未删除。计划原已把删除动作放在 P8-09；本层不为获得漂亮数字而提前移除风险标记，也不谎称生产已切换。

## 验证

- P8-02 离线验收：8 项测试、10 个结构化样例全部通过；
- 完整回归：493 项运行成功，490 项通过，3 项为明确保留的 v1 expected failure；
- `git diff --check`：通过；
- 未部署，未执行 Git/GitHub 写操作。

## 下一入口

P8-03：建立本地端到端模拟矩阵，组合验证主人/群友、私聊/群聊、普通聊天/联网只读工具、表情候选、单/多气泡、引用和换人；同时核对每个场景的目标、能力、调用预算和发送预算。
