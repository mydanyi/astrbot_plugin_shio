# AstrBot 思考模型：真实私聊测试

时间：2026-09-07，17:58～18:04（香港时间）。

## 新增配置

- 新增模型条目：`V100/qwen3.8-27b-nvfp4-dflash2-thinking`。
- 复用现有本地 V100 来源，实际模型名仍为 `qwen3.8-27b-nvfp4-dflash2`；公开版省略私有服务地址。
- 唯一推理参数差异：`custom_extra_body.chat_template_kwargs.enable_thinking=true`。
- 未改 V100 服务或默认关闭思考的设置；未修改原 AstrBot 模型配置或全局默认模型。
- 测试通过 AstrBot 官方会话偏好临时选中新增模型。测试后恢复两个私聊原来的选择，新增模型保留。
- 本轮未改 Shio、AstrBot 或其它插件源码，人设及 Meme 配置保持本轮开始时的设置。

## 实际 QQ 结果

所有测试均为普通聊天，没有在消息中索要表情包。请求日志核实四轮均携带思考开关，账号身份区分正确。

| 测试 | 收到的文字 | 收到的图片 | 首条文字耗时 |
| --- | --- | --- | --- |
| 主人 A，第 1 条 | 2 个气泡 | 0 | 21.55 秒 |
| 主人 A，第 2 条 | 1 个气泡 | 1 | 31.17 秒 |
| 群友 B，第 1 条 | 1 个气泡 | 0 | 18.68 秒 |
| 群友 B，第 2 条 | 1 个气泡 | 0 | 15.74 秒 |

主人第二条实际调用了 `search_memes`，随后发送文字和图片。从主人测试账号接收端再次调用 OneBot `get_login_info`、`get_msg`，确认：

- 接收账号：主人测试账号。
- 发送账号：机器人测试账号。
- 会话类型：private。
- 送达时间：**2026-09-07 18:01:16 +08:00**。
- 图片与前序文字均从接收端回读确认。公开版不包含账号、消息编号或个性化称呼。

上一轮成功的“私聊明确索图”属于群友 B，并非主人 A。此处是首次记录主人接收端回读确认的私聊图片，不能混用两账号的证据。

## 结论

新增思考模型在真实主人私聊中出现了主动配图，但不是每条都会配图。本轮为 4 条中 1 条有图，样本不足以评估长期频率，不能据此标记“私聊主动配图完全修复”。

三个没有图片的场景都正常收到了文字；因为测试要求必须收图，其图片断言返回失败。不是三条聊天请求报错。测试中的回复仍沿用了之前考试、蛋糕等模拟聊天内容，已有历史/记忆的影响没有因切模型消失。

测试脚本、原会话选择及结果保留于私有任务资料中，不提交本地路径、生产配置或认证资料。一次性测试容器使用 `--rm` 自动退出，生产及两个 NapCat 测试账号容器保留。
